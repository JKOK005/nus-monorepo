package Raft

import (
	"fmt"
)

func getLeader() (string, int, error) {
	/*
		Entry point for RAFT. Nodes will start as slaves until timeout before moving to candidate state.
	*/
}

func Init() error {
	/*
		Entry point for RAFT. Nodes will start as slaves until timeout before moving to candidate state.
	*/
}
